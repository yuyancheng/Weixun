'use strict';

app.controller('OrgRemove', function($rootScope, $scope, $state) {
	//$scope.details = $rootScope.details;
/*	$scope.goBack = function(){
		$state.transitionTo('app.org', true);
	};*/
});